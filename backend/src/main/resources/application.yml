micronaut:
  server:
    cors:
      enabled: true
      configurations:
        web:
          allowed-origins:
            - http://localhost:3000
            - https://money-map-liard-phi.vercel.app
          allowed-methods:
            - GET
            - POST
            - PUT
            - PATCH
            - DELETE
            - OPTIONS
          allowed-headers:
            - Content-Type
            - Authorization
  security:
    enabled: true
    intercept-url-map:
      - pattern: /api/v1/auth/**
        access:
          - isAnonymous()

      - pattern: /api/v1/budget/**
        access:
          - isAuthenticated()

      - pattern: /api/v1/recurring-expenses
        access:
          - isAuthenticated()

      - pattern: /api/v1/recurring-expenses/**
        access:
          - isAuthenticated()

    token:
      jwt:
        enabled: true
        generator:
          access-token:
            expiration: 3600
        signatures:
          secret:
            generator:
              secret: ${JWT_SECRET}

datasources:
  default:
    url: ${DATABASE_URL:`jdbc:postgresql://localhost:6543/moneymap`}
    driverClassName: org.postgresql.Driver
    username: ${DATABASE_USERNAME:moneymap}
    password: ${DATABASE_PASSWORD:moneymap}
    dialect: POSTGRES

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update

flyway:
  datasources:
    default:
      enabled: true